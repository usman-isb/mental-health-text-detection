# ── Python ────────────────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store

# ── Data (downloaded/regenerated by notebooks 00–02; too large for git) ───────
datasets/raw/
datasets/processed/*
# keep the label encoder — tiny, and the webapp needs it without re-running notebooks
!datasets/processed/splits/
datasets/processed/splits/*
!datasets/processed/splits/label_encoder.pkl

# ── Models (regenerated by notebooks 04–06; fine-tuned BERT is on HF Hub:
#    https://huggingface.co/usman-isb/bert-mental-health-detection) ────────────
models/saved/

# ── Secrets ───────────────────────────────────────────────────────────────────
.env
*.token
kaggle.json

# ── Logs / temp ───────────────────────────────────────────────────────────────
*.log
