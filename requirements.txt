# ── PyTorch ───────────────────────────────────────────────────────────────────
# Intel Macs (x86_64): PyTorch stopped publishing macOS-Intel wheels after 2.2.2,
# and torch 2.2 must run with numpy 1.x and transformers 4.x.
# Use Python 3.10–3.12 on Intel Macs (3.13 has no torch wheel there).
torch==2.2.2; sys_platform == "darwin" and platform_machine == "x86_64"
torch>=2.0.0; sys_platform != "darwin" or platform_machine != "x86_64"

numpy<2; sys_platform == "darwin" and platform_machine == "x86_64"
numpy>=1.24.0; sys_platform != "darwin" or platform_machine != "x86_64"

transformers>=4.44.0,<5; sys_platform == "darwin" and platform_machine == "x86_64"
transformers>=4.30.0; sys_platform != "darwin" or platform_machine != "x86_64"

# ── ML / data ─────────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=1.7.0
shap>=0.42.0
lime>=0.2.0.1
nltk>=3.8.0
spacy>=3.6.0
pandas>=2.0.0
imbalanced-learn>=0.11.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Web application ───────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.23.0
streamlit>=1.40.0          # st.navigation (multi-page) needs >= 1.36
pyspellchecker>=0.8.0      # input validation in the frontend
requests>=2.31.0
python-multipart>=0.0.6

# ── Data acquisition ──────────────────────────────────────────────────────────
kaggle>=1.5.0
