# ── Streamlit Community Cloud deployment (web app only) ──────────────────────
# This branch (streamlit-deploy) slims the dependencies to what the web app
# needs at runtime. The full research environment lives on the main branch.
#
# torch is pinned to a CPU-only wheel: the default Linux wheel bundles CUDA
# (~5 GB) and would exceed the Community Cloud build limits.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.1+cpu

transformers>=4.44.0
numpy>=1.24.0
scikit-learn>=1.3.0          # unpickling label_encoder.pkl
shap>=0.42.0
lime>=0.2.0.1
pandas>=2.0.0
matplotlib>=3.7.0
streamlit>=1.40.0            # st.navigation (multi-page) needs >= 1.36
pyspellchecker>=0.8.0        # input validation in the frontend
requests>=2.31.0
